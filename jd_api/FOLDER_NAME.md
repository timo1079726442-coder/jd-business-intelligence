# 📂 文件夹命名说明：jd_api

## 基本信息

| 项 | 内容 |
|---|------|
| **文件夹名** | `jd_api` |
| **命名含义** | "京东（JD）" + "API" 的下划线连接形式 |
| **完整含义** | 京东API代码包（已废弃，遗留目录） |
| **位置** | 项目根目录 |
| **创建原因** | 早期设计：每个京东业务用独立py文件实现 |

## ⚠️ 当前状态

**此目录已被废弃**，仅作历史归档保留。

- 远程GitHub commit `bc331c7` 已重构：所有代码合并到 `main.py`
- 当前所有业务代码都在 `main.py` 一个文件里
- `__pycache__/` 子目录是Python编译缓存，可忽略
- 本目录目前基本是空的（远程git pull后文件被删除，只剩目录）

## 命名规范

✅ 早期命名：用下划线分隔的Python包名（`jd_api`）
⚠️ 按全局铁律第4条"代码集成规范"：**所有接口代码集中在 main.py**，不拆分py文件
❌ 此目录不再新增代码

## 历史文件（已删除）

- `jd_api/base.py` - 通用请求基类（已合并到 `main.py`）
- `jd_api/shop_source.py` - 商品搜索效果API实现（已合并到 `main.py`）
- `jd_api/__init__.py` - 包初始化（已合并到 `main.py`）