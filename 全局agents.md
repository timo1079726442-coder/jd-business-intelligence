# 全局agents.md

> 本文档记录每次与AI助手对话的改动内容、改动逻辑，用通俗易懂的方式说明。

---

## 🚨 全局铁律（京东商智API自动化项目 - 2026-08-04 制定）

**本规则适用于本项目后续所有接口任务，必须严格遵守。**

### 1️⃣ 项目归档规则
单个项目开发调试全部完成后，将本项目过程经验、踩坑点、解决方案，统一写入 `agents.md`，作为全局知识库留存。

### 2️⃣ 迭代规划
当前仅完成第一个项目，后续会持续提供更多API接口需求，需要和用户逐项对齐完成。

### 3️⃣ 信息对齐要求【重要】
当输入信息描述模糊、需求不明确、上下文信息缺失，导致无法准确对齐需求时，**优先向用户提出疑问，确认清楚后再执行开发，禁止自行猜测直接输出代码**。

### 4️⃣ 代码集成规范
所有项目接口**不拆分大量独立py文件**；调试验证接口逻辑无误后，直接合并集成进现有主程序模块，项目代码按业务名称命名，例：第一个项目命名：`商品搜索效果`。

### 5️⃣ 文档输出强制要求
- 每完成一个API接口项目，必须把接口完整实现逻辑写入 `API实现逻辑说明.md`；
- 每完成一个API接口项目，把踩坑经验、问题解决方案更新至全局文档 `agents.md`。

### 6️⃣ 接口分析强制要求
每一个API接口，必须完整分析输出：
- 静态固定参数
- **全部动态参数**（动态参数范围包含：请求头、请求体、表单参数）
- **Cookie内部的动态/风控变量也必须识别标注**

---

## 第一个项目：商品搜索效果（已完成 2026-08-04）

### 项目业务说明
- 业务名：`商品搜索效果`
- 接口：`https://szgateway.jd.com/szpaas/szajax/shop/source/offlineFlowSource/downSkuTable.ajax`
- 功能：导出店铺来源中"搜索"渠道的SKU维度流量数据
- 输出：Excel文件（按入店浏览量降序，最多5000条SKU）

### 当前实现文件
- `jd_api/base.py` - 通用请求基类（风控签名、间隔、重试、UA切换、配置加载）
- `jd_api/shop_source.py` - `商品搜索效果` API实现
- `main.py` - 主程序入口
- `config/config.xlsx` - 项目配置（全局+商品搜索效果参数）
- `docs/API 实现逻辑说明.md` - 接口实现逻辑文档
- `create_config_xlsx.py` - 配置文件生成脚本

### 项目经验沉淀
见下文各次对话记录。

---

## 2026-08-04 第一次对话

### 任务目标
测试京东商智API接口 `downSkuTable.ajax`，验证是否为店铺来源中的搜索流量数据。

---

### 改动1：创建项目配置文件

**文件**：`config/cookie.txt`

**改动内容**：
- 把你提供的京东登录Cookie保存到了 `config/cookie.txt` 文件中

**改动逻辑**：
- Cookie是京东识别你身份的凭证，相当于你的"登录通行证"
- 不直接写在代码里，而是放在单独的文件中，原因是：
  1. Cookie会过期（几小时到几天），需要经常更新，放文件里方便替换
  2. 避免Cookie混在代码里不好管理
  3. 上传GitHub时可以排除这个文件，保护账号安全

---

### 改动2：逆向破解京东风控签名算法

**改动内容**：
- 下载并分析了京东商智前端JS文件（`commons-a5562705.js`）
- 找到了 `User-mnp` 签名的生成算法

**改动逻辑**：
- 京东商智的API有防爬虫机制，直接调用会返回"不安全的请求"
- 需要带上三个风控参数：`User-mup`（时间戳）、`User-mnp`（MD5签名）、`uuid`（随机标识）
- 其中 `User-mnp` 的计算公式是：
  ```
  User-mnp = MD5(URL路径 + uuid + 当前时间戳 + "372ad2c2b6")
  ```
- 通俗解释：就像你去银行办业务，工作人员要核对你的身份证号+当前时间+一个暗号，组合在一起做一个加密处理，生成一个"防伪码"。京东服务端收到请求后用同样的算法验证这个码对不对，对的话才给你数据。

**关键参数说明**：
| 参数 | 作用 | 怎么生成 |
|------|------|----------|
| URL路径 | 请求的接口地址路径 | 固定值 `/szpaas/szajax/shop/source/offlineFlowSource/downSkuTable.ajax` |
| uuid | 随机标识符 | 随机生成，格式 `ca412182e5668a106054-数字` |
| 时间戳 | 当前时间 | 代码自动获取当前毫秒时间 |
| `372ad2c2b6` | 固定盐值（密钥） | 京东前端写死的，从JS代码中提取 |

---

### 改动3：API测试成功，返回搜索流量Excel数据

**文件**：`test_api6.py`（测试脚本）、`output/搜索流量_2026-08-03.xlsx`（输出结果）

**改动内容**：
- 编写完整的API调用脚本，包含签名生成、请求发送、Excel保存和读取分析
- 成功获取到2026-08-03的搜索流量数据

**返回数据说明**：
- 文件格式：Excel (.xlsx)
- 工作表名：`店铺来源_商品效果`
- 数据量：45条SKU记录 × 68列字段
- 主要字段包括：
  - 商品名称、商品SKU
  - 访客数、访客数环比、访客数占比
  - 浏览量、浏览量环比、人均浏览量
  - 平均停留时长、UV价值
  - 加购客户数、加购转化率
  - 成交客户数、成交转化率、成交金额
  - 客单价、下单客户数、下单金额
  - SPU维度数据（加购/成交/下单等）
  - 店铺关注人数
  - 新老客户数据（整体/180天/两年）

**确认结论**：
✅ 这个API接口确实是**店铺来源 → 搜索流量 → SKU维度数据**，`lastSrcChannelId1=2` 对应搜索渠道。

---

### 改动4：创建项目规范文档

**文件**：
1. `工作区命名与架构规范.md` - 项目架构和命名规范
2. `全局复利的踩坑日志.md` - 记录踩过的坑和分歧
3. `全局agents.md` - 本文件，记录每次改动内容

**改动逻辑**：
- 按照你的要求，建立项目文档体系
- 方便后续多接口开发时保持一致的代码风格和架构
- 踩坑日志避免重复犯同样错误

---

### 临时测试文件说明

测试过程中产生了以下临时文件，后续会清理：
- `test_api.py` ~ `test_api6.py`：API测试脚本（迭代调试用）
- `jmsdkPC.min.js`、`commons.js`、`vendors.js`、`main-1a73b092.js`：下载的京东前端JS文件（用于逆向分析）

---

## 2026-08-04 第二次对话

### 任务目标
为每个API接口创建详细的API清单Excel，包含字段含义、类型、风险等级等信息，供用户核查和AI自查。

---

### 改动5：创建项目API清单Excel

**文件**：`docs/项目API清单.xlsx`、`create_api_list.py`（生成脚本）

**改动内容**：
创建了一个包含5个工作表的API清单Excel文件：

| 工作表 | 内容 | 数据量 |
|--------|------|--------|
| API接口总览 | 每个接口一行，含接口名/URL/域/触发点/频率/风险等级等15列 | 1条 |
| 字段明细 | 每个参数一行，含字段名/含义/类型(硬编码/动态/风控)/处理方式/理由等11列 | 31条 |
| 返回字段说明 | 返回Excel的每列含义，68列字段逐个注明 | 68条 |
| 风控参数说明 | User-mup/User-mnp/uuid三个风控参数的算法公式、盐值、来源 | 3条 |
| 返回字段分类 | 68列字段按12个分类归纳（流量/加购/成交/下单/新老客等） | 12条 |

**改动逻辑**：
- 你要求每个API接口的分列注明字段含义、硬编码/动态参数、域、触发点、频率、风险等级等
- 我在基础上补充了：返回格式、单次返回上限、是否必须、备注、风控算法公式、失败现象、解决方案等
- 做成Excel而不是文本，方便你直接打开核查、筛选、排序
- 后续每给你一个新API接口，我都会往这个Excel里追加一行

**列说明（通俗解释）**：
- **字段类型**：硬编码=固定写死不变；动态参数=每次查询要改；风控参数=必须用算法生成；配置化=有默认值但可改
- **触发点**：什么情况下你会用到这个接口（比如"需要导出搜索流量时"）
- **单次放大**：一次请求最多返回多少数据（这个接口是5000条SKU）
- **风险等级**：高=签名错误直接被拦；中=频繁请求可能被拦；低=一般不会出问题
- **触发场景**：什么操作会导致被京东风控（比如间隔太短、Cookie过期）

---

## 2026-08-04 第三次对话

### 任务目标
1. 创建《API实现逻辑说明.md》文档，写明字段映射、动态参数来源、风控处理流程
2. 编写通用Python请求类 base.py（30秒间隔、重试机制、日志记录），供后续接口复用

---

### 改动6：创建API实现逻辑说明文档

**文件**：`docs/API实现逻辑说明.md`

**改动内容**：
- 详细列出了第一个接口的完整字段映射表（Header 11个 + Body业务参数 13个 + Body风控参数 3个）
- 画了动态参数来源示意图（用户输入 / 代码生成 / 配置文件 / 代码常量）
- 写了风控参数生成的6步流程（时间戳→uuid→URL路径→拼接→MD5→组装）
- 写了请求完整流程（9步：读Cookie→组装参数→生成风控→间隔控制→发送→检查→日志→返回）
- 画了代码结构预览（base.py + shop_source.py 的类/方法设计）

**改动逻辑**：
- 按你的要求，先写文档让你确认，确认无误后再写代码
- 把每个参数"从哪来、怎么处理、为什么这么做"都写清楚

---

### 改动7：创建通用请求基类 base.py

**文件**：`jd_api/base.py`、`jd_api/__init__.py`、`test_base.py`

**改动内容**：
创建了一个 `JDBaseRequest` 通用类，封装了以下功能：

| 功能 | 实现方式 | 通俗解释 |
|------|----------|----------|
| Cookie管理 | 从config/cookie.txt自动读取，提供refresh_cookie()更新方法 | 像"通行证"一样，自动读取，过期了可以一键更新 |
| 风控签名生成 | _gen_risk_params()自动生成User-mup/mnp/uuid | 每次请求自动算出"防伪码"，不用手动管 |
| 30秒间隔控制 | _wait_interval()自动等待补齐 | 两次请求之间不够30秒会自动等，防止被京东封 |
| 重试机制 | 最多重试3次，递增等待（30秒→60秒→90秒） | 失败了自动重试，越等越久给京东"消气" |
| 日志记录 | 同时写入文件和控制台，按日期命名 | 像"日记本"一样记录每次请求的时间和结果 |
| Excel保存 | save_excel()直接保存返回的二进制数据 | API返回的Excel数据自动存到output/目录 |

**测试结果**：
- ✅ 初始化成功（Cookie读取、目录创建、日志初始化）
- ✅ 风控参数生成正常
- ✅ API调用成功（HTTP 200，16176字节Excel）
- ✅ Excel保存成功

**代码结构**：
```
JDBaseRequest (base.py)
├── __init__()           → 初始化：读Cookie、建目录、设日志
├── _read_cookie()       → 读cookie.txt
├── refresh_cookie()     → 更新Cookie
├── _gen_risk_params()   → 生成风控三参数
├── _wait_interval()     → 30秒间隔控制
├── _build_default_headers() → 组装请求头
├── request()            → 通用请求(含重试)
├── save_excel()         → 保存Excel
└── _init_logger()       → 初始化日志
```

**后续接口复用方式**：
```python
from jd_api.base import JDBaseRequest

class ShopSourceAPI(JDBaseRequest):
    def download_search_sku(self, date):
        data = {"date": date, ...}
        response = self.request(url, data)  # 自动处理风控+间隔+重试
        return self.save_excel(response, f"搜索流量_{date}.xlsx")
```

---

## 更新记录

| 日期 | 改动概要 |
|------|----------|
| 2026-08-04 | 初始化项目，破解京东风控签名，API测试成功，创建规范文档 |
| 2026-08-04 | 创建项目API清单Excel，含5个工作表共115条记录 |
| 2026-08-04 | 创建API实现逻辑说明文档 + 通用请求基类base.py（含30秒间隔/重试/日志），测试通过 |
| 2026-08-04 | 加入Edge/Chrome双UA切换机制 + 编写shop_source.py接口实现 + main.py主程序入口，测试通过 |
| 2026-08-04 | uuid方案调查：通过API获取不可行，京东用运行时SDK动态生成uuid，无法从静态JS还原。保留"固定前缀+随机数"方案并在代码中加详细说明 |

---

## 2026-08-04 第四次对话

### 任务目标
1. 编写具体接口实现代码（shop_source.py），复用base.py的通用能力
2. Edge/Chrome UA都写入，Edge失败自动切换Chrome
3. 每次写入文件前加改动总结
4. 提供签名验证失败的排查指南

---

### 改动8：base.py 加入双UA切换机制

**文件**：`jd_api/base.py`

**改动内容**：
- 增加 `UA_EDGE` 和 `UA_CHROME` 两个常量
- 新增 `_ua_list` 列表（存两组UA+对应sec-ch-ua）和 `_current_ua_index` 状态
- 新增 `_switch_ua()` 方法（Edge ↔ Chrome 切换）
- `_build_default_headers()` 改为读取当前UA动态组装
- `request()` 方法在检测到风控拦截时调用 `_switch_ua()` 后再重试
- 修复了原代码 `e = TimeoutError(...)` 的bug（`e` 未在except定义）

**改动逻辑（通俗版）**：
- 你要求Edge和Chrome UA都写入，失败时切换
- 就像你有两件衣服（Edge外套、Chrome外套）
- 第一次请求穿Edge（和你的抓包一致），如果被拦就自动换Chrome再试

---

### 改动9：编写 shop_source.py 接口实现

**文件**：`jd_api/shop_source.py`

**改动内容**：
- 创建 `ShopSourceAPI` 类，继承自 `JDBaseRequest`
- 核心方法 `download_sku()`：通用方法，支持任意渠道（搜索/首页/类目等）
- 便捷方法 `download_search_sku()`：调用download_sku()的快捷方式
- 常量设计：
  - `API_URL`：接口地址固定
  - `CHANNEL_MAP`：渠道ID映射表（搜索=2/2008）
  - `SORT_FIELDS`：排序字段映射表（中文名→英文字段名）

**改动逻辑（通俗版）**：
- base.py 是"工具箱"
- shop_source.py 是"工人"
- 工人用工具箱的工具去京东拿数据
- 就像你给工人一个工具箱，但没告诉他做什么，我写了个具体任务："去京东拿搜索流量数据"

---

### 改动10：编写 main.py 主程序入口

**文件**：`main.py`

**改动内容**：
- 用户的"启动按钮"
- 调用 `download_search_sku(date="2026-08-03")` 导出搜索流量数据

**使用方法**：直接运行 `python main.py`，或修改 `main()` 中的 `date` 变量改成其他日期

**改动逻辑（通俗版）**：
- base.py = 工具箱
- shop_source.py = 工人
- main.py = 按钮，按一下工人开始干活

---

### 问题3答案：签名验证失败的排查指南

**如果后续发现 `User-mnp` 签名验证失败，按以下顺序排查**：

#### 排查步骤

| 步骤 | 检查项 | 排查方法 | 解决方式 |
|------|--------|----------|----------|
| 1 | 时间戳是否新鲜 | 看日志中的 `User-mup` 值是不是当前时间（毫秒级） | 如果是几小时前的，说明时间戳生成逻辑坏了 |
| 2 | UUID格式 | 看日志中的 `uuid`，应该是 `ca412182e5668a106054-数字` 格式 | 如果格式不对，检查 `UUID_PREFIX` 常量 |
| 3 | URL路径处理 | 手动算一下MD5，看和 `User-mnp` 是不是一样 | 用下面的Python代码手动算 |
| 4 | 盐值是否正确 | 检查 `SIGN_SALT` 常量是不是 `372ad2c2b6` | 直接对比代码常量 |

#### 手动验证MD5签名的Python代码

```python
import hashlib

# 复制日志中的3个值
url_path = "/szpaas/szajax/shop/source/offlineFlowSource/downSkuTable.ajax"  # ← 从URL手动提取
uuid_str = "ca412182e5668a106054-1234567890"  # ← 从日志复制
timestamp = "1785828813987"  # ← 从日志复制
salt = "372ad2c2b6"

# 拼接 + MD5
sign_str = f"{url_path}{uuid_str}{timestamp}{salt}"
expected = hashlib.md5(sign_str.encode("utf-8")).hexdigest()
print(f"期望签名: {expected}")
# 和日志中的 User-mnp 对比，如果不一致就是拼接顺序错了
```

#### 拼接顺序可能出错的情况

如果按以上代码算出来的 `expected` 和日志中 `User-mnp` 不一致，尝试以下拼接顺序（因为vendors.js中位置1333275的代码用的是相反顺序）：

```
顺序1（当前用的，多数情况）：url_path + uuid + timestamp + salt
顺序2（备用）：uuid + url_path + timestamp + salt
顺序3（备用）：url_path + timestamp + uuid + salt
```

#### 排查流程图

```
签名验证失败
  │
  ├─ Step1: 看日志中3个风控参数是否都存在？
  │    ├─ 缺少 → 检查 _gen_risk_params() 是否被调用
  │    └─ 都有 → 进入Step2
  │
  ├─ Step2: 手动计算MD5，对比日志中的User-mnp
  │    ├─ 一致 → 签名算法没问题，是其他原因（如Cookie过期）
  │    └─ 不一致 → 拼接顺序错了，尝试其他顺序
  │
  └─ Step3: 用排除法
       ├─ 换一个URL试试（如首页接口）
       ├─ 换一个盐值试试（372ad2c2b6 / 372ad2c2b6...）
       └─ 重新去京东商智页面抓一次最新的JS代码对比盐值
```

---

## 文档变更记录

每次对话都更新本文件，防止后续AI失忆。

---

## 2026-08-04 第五次对话

### 任务目标
把不确定/会变的变量通过API获取后再用，而不是硬编码

### 调查结果

#### 用户的想法
- 盐值、uuid 等不确定或会变的变量，应该先通过API获取，再把值套进请求体
- 这样如果京东后端改了生成逻辑，我们只需要改获取方式，不用改签名算法

#### 我做的调查

| 调查项 | 结论 |
|--------|------|
| HTML页面里有uuid？ | ❌ 没有（页面HTML只有空壳） |
| jmsdkPC.min.js里有uuid生成？ | ❌ 没有（只有jmapp相关） |
| sgm-web-4.2.0.js里有uuid生成？ | ❌ 没有（被webpack极简化） |
| commons.js里有ca412182字符串？ | ❌ 没有 |
| vendors.js里有？ | ⚠️ 有引用但生成逻辑不在这 |
| **结论** | **uuid是前端SDK运行时动态生成，无法从静态JS还原** |

#### 为什么不能通过API获取uuid？

1. **uuid由浏览器运行时生成**：京东的 `window.$frontend_monitor.getUUID(url)` 是JS在浏览器中执行的函数
2. **没有公开API**：京东没有提供独立的uuid生成HTTP接口
3. **静态JS里没有算法**：所有可下载的JS文件都不包含完整的uuid生成逻辑
4. **唯一可靠方式**：用selenium/playwright启动真实浏览器，让JS自己跑起来拿uuid

#### 当前方案（保留+优化）

- **保留**：`固定前缀 + 10位随机数` 方案
- **原因**：测试已通过，实际API调用拿到了真实数据
- **风险**：uuid后半段格式可能不完全匹配，但目前有效
- **优化**：在 `base.py` 中加详细注释，说明限制和未来升级方案

#### 改动11：base.py 加注释

**文件**：`jd_api/base.py` 的 `_gen_risk_params()` 方法

**改动内容**：
```python
# 2. 生成uuid（固定前缀 + 10位随机数字）
# 注：京东真实uuid是由前端SDK运行时动态生成（ca412182e5668a106054-xxx），
#    无法从静态JS文件中还原算法。当前用"固定前缀+随机数"模拟，测试通过。
#    如果后续被拦截，考虑使用浏览器自动化(selenium/playwright)获取真实uuid。
uuid_str = f"{self.UUID_PREFIX}-{random.randint(1000000000, 9999999999)}"
```

**改动逻辑**：
- 你要求"不确定变量通过API获取"
- 但调查后发现uuid没法API获取（只能在浏览器运行时生成）
- 所以保留现有方案，但在代码里加详细说明
- 防止后续AI不知道这个限制

#### 未来升级方向（如果uuid被拦）

| 方案 | 实施难度 | 准确度 |
|------|---------|--------|
| **selenium/playwright**：启动真实Chrome，访问商智页面提取uuid | ⭐⭐ 中 | ✅ 100%准确 |
| **继续观察**：先用着，看会不会被拦 | ⭐ 易 | ⚠️ 不确定 |
| **逆向JM SDK**：继续追踪jmsdk内部uuid算法 | ⭐⭐⭐ 难 | ⚠️ 可能还原 |
