# 工作区命名与架构规范

> 本文档用于区分项目，规范文件命名、目录结构和代码组织方式。

---

## 一、项目信息

| 项目名称 | FYA箱包旗舰店京东后台数据导出 |
|----------|-------------------------------|
| 项目目录 | `d:\CODE\trae\traespace\FYA箱包旗舰店` |
| 创建日期 | 2026-08-04 |
| 店铺名称 | FYA8888 |
| 平台 | 京东商智 (sz.jd.com) |

---

## 二、目录结构规范

```
FYA箱包旗舰店/
├── config/                     # 配置文件目录
│   ├── cookie.txt              # 京东登录Cookie（动态更新，不入Git）
│   └── config.xlsx             # 项目配置（全局+商品流量来源：日期/间隔/盐值/可变参数）
├── output/                     # 导出的数据文件目录（已被.gitignore排除）
│   └── 搜索流量_2026-07-30.xlsx # 命名：{渠道}流量_{日期}.xlsx
├── logs/                       # 运行日志目录（按日期）
├── docs/                       # 文档目录
│   ├── API 实现逻辑说明.md      # 接口实现逻辑（字段映射/签名/踩坑/变更记录）
│   ├── 工作区命名与架构规范.md
│   └── 全局复利的踩坑日志.md
├── .trae/skills/               # Trae技能（按业务领域划分）
│   ├── jd-api-analyze/         # 京东全系业务接口分析（商智/京麦/京准通）
│   ├── git-safe-operate/       # Git安全操作
│   └── python-code-gen/        # Python代码生成规范
├── main.py                     # ⭐ 唯一主程序入口（所有代码集中于此）
├── main_old_backup.py          # 旧版存档+业务上下文备份（仅人工查阅，不作为运行入口）
├── jd_api/                     # ⚠️ 已废弃目录（代码已合并进main.py，仅存说明文件）
└── AGENTS.md / agents.md       # Trae Agent全局规则（只放底线规则，禁止堆业务）
```

> 说明（2026-08-05）：项目已按全局铁律改为**单文件集中式**架构，全部代码在 `main.py`，不再拆分 jd_api/*.py。

---

## 三、文件命名规范

### 3.1 Python代码文件
- **单文件集中式**：业务代码统一放 `main.py`（全局铁律第4条），不拆分多个独立py文件
- 模块内用 `============` 分层注释隔离（基类 / 各业务API / 注册中心 / 主入口）
- 临时测试脚本以 `_` 开头（如 `_mock_test.py`），用完即删

### 3.2 导出的数据文件
- 格式：`{渠道}流量_{日期}.xlsx`
- 示例：`搜索流量_2026-07-30.xlsx`、`推荐流量_2026-07-30.xlsx`、`购物车流量_2026-07-30.xlsx`
- 网页对照文件加前缀：`网页_搜索流量_2026-07-30.xlsx`

### 3.3 配置文件
- 纯文本格式，如 `cookie.txt`
- 敏感信息不入代码，统一放 `config/` 目录

---

## 四、代码架构规范

### 4.1 分层设计（2026-08-05 更新）
全部集中在 `main.py` 内分层：
1. **配置层**：`config/config.xlsx`（Cookie路径/间隔/盐值/可变参数），Cookie在 `config/cookie.txt`
2. **基础层** `class JDBaseRequest`：风控签名、30秒间隔、重试、UA切换、Cookie管理、日志、Excel保存
3. **工具层** 公共函数：`convert_date_format()` / `safe_convert_numeric()` / `apply_column_formats()`
4. **业务层** `class ProductFlowAPI`：商品流量来源（搜索2008/推荐2009/购物车3001）
5. **调度层** `BUSINESS_REGISTRY` + `run_business()`：新业务只注册，不改调度核心
6. **入口层** `main()`：命令行 `--biz_key / --date / --start_date / --end_date / --list`

### 4.2 API接口封装规范
每个API接口需注明：
- 接口URL和请求方式
- 每个参数的含义、类型（动态/硬编码）、理由
- 返回数据格式说明
- 调用间隔要求

### 4.3 风控安全规范
- **请求间隔**：所有API调用之间必须间隔 ≥ 30秒
- **签名算法**：`User-mnp = MD5(URL路径 + uuid + 时间戳 + "372ad2c2b6")`
- **Cookie管理**：从 `config/cookie.txt` 读取，不硬编码在代码中

---

## 五、版本管理规范

- 代码同步到GitHub，确保多设备进度一致
- `.gitignore` 排除 `config/cookie.txt`（敏感信息不上传）
- 提交信息用中文，说明本次改动内容

---

## 六、更新记录

| 日期 | 更新内容 |
|------|----------|
| 2026-08-04 | 创建初始规范文档 |
| 2026-08-05 | 更新：单文件集中式架构（main.py），目录结构/分层设计/命名规范同步（jd_api废弃）|
