# 工作区命名与架构规范

> 本文档用于区分项目，规范文件命名、目录结构和代码组织方式。

---

## 一、项目信息

| 项目名称 | FYA箱包旗舰店京东后台数据导出 |
|----------|-------------------------------|
| 项目目录 | `d:\CODE\trae\traespace\FYA箱包旗舰店` |
| 创建日期 | 2026-08-04 |
| 店铺名称 | FYA8888 |
| 平台 | 京东商智 (sz.jd.com) |

---

## 二、目录结构规范

```
FYA箱包旗舰店/
├── config/                     # 配置文件目录
│   └── cookie.txt              # 京东登录Cookie（动态更新）
├── output/                     # 导出的数据文件目录
│   └── 搜索流量_2026-08-03.xlsx # 按类型_日期命名
├── jd_api/                     # API接口模块
│   ├── __init__.py
│   ├── base.py                 # 基础请求类（含风控签名、30秒间隔控制）
│   └── shop_source.py          # 店铺来源相关API
├── docs/                       # 文档目录
│   ├── 工作区命名与架构规范.md
│   ├── 全局复利的踩坑日志.md
│   └── 全局agents.md
├── main.py                     # 主程序入口
└── README.md                   # 项目说明（如有需要）
```

---

## 三、文件命名规范

### 3.1 Python代码文件
- 全小写 + 下划线，如 `shop_source.py`、`base.py`
- 测试脚本以 `test_` 开头，如 `test_api.py`

### 3.2 导出的数据文件
- 格式：`{数据类型}_{日期}.xlsx`
- 示例：`搜索流量_2026-08-03.xlsx`、`首页流量_2026-08-03.xlsx`
- 多日汇总：`搜索流量_2026-08-01_2026-08-03.xlsx`

### 3.3 配置文件
- 纯文本格式，如 `cookie.txt`
- 敏感信息不入代码，统一放 `config/` 目录

---

## 四、代码架构规范

### 4.1 分层设计
1. **配置层** (`config/`)：Cookie、店铺参数等动态配置
2. **基础层** (`jd_api/base.py`)：封装通用请求逻辑
   - 风控签名生成（User-mnp MD5算法）
   - 30秒请求间隔控制（防风控）
   - Cookie读取与管理
   - 通用请求头管理
3. **接口层** (`jd_api/shop_source.py` 等)：按功能模块封装具体API
4. **入口层** (`main.py`)：用户操作入口

### 4.2 API接口封装规范
每个API接口需注明：
- 接口URL和请求方式
- 每个参数的含义、类型（动态/硬编码）、理由
- 返回数据格式说明
- 调用间隔要求

### 4.3 风控安全规范
- **请求间隔**：所有API调用之间必须间隔 ≥ 30秒
- **签名算法**：`User-mnp = MD5(URL路径 + uuid + 时间戳 + "372ad2c2b6")`
- **Cookie管理**：从 `config/cookie.txt` 读取，不硬编码在代码中

---

## 五、版本管理规范

- 代码同步到GitHub，确保多设备进度一致
- `.gitignore` 排除 `config/cookie.txt`（敏感信息不上传）
- 提交信息用中文，说明本次改动内容

---

## 六、更新记录

| 日期 | 更新内容 |
|------|----------|
| 2026-08-04 | 创建初始规范文档 |
