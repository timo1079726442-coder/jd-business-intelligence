# 全局复利的踩坑日志

> 本文档记录对话过程中所有分歧、不满意、需要重新改的地方，避免重复踩坑。

---

## 踩坑记录

### 坑1：API请求被京东风控拦截 - "不安全的请求"

**时间**：2026-08-04

**问题描述**：
直接用Python requests调用京东商智API `downSkuTable.ajax`，返回 `{"message": "不安全的请求", "status": -407}`。

**尝试过程**：
1. ❌ 方案A：去掉 `User-mup`、`User-mnp`、`uuid` 三个风控参数 → 仍然被拦截（status: -402）
2. ❌ 方案B：用Session先访问页面再调API，用原始抓包的固定 `User-mnp` 值 → 仍然被拦截（status: -407）
3. ✅ 方案C：逆向分析京东商智前端JS代码，找到签名算法

**根本原因**：
京东商智的API有前端风控签名校验，`User-mnp` 不是随机值，而是用特定算法生成的MD5签名。固定值或随机值都通不过校验。

**解决方案**：
通过下载分析 `commons-a5562705.js` 文件，找到签名算法：
```
User-mnp = MD5(URL路径 + uuid + 时间戳 + "372ad2c2b6")
```
- URL路径：`/szpaas/szajax/shop/source/offlineFlowSource/downSkuTable.ajax`
- uuid：随机生成（格式：`ca412182e5668a106054-xxxxxxxxxx`）
- 时间戳：当前毫秒时间戳
- `372ad2c2b6`：固定盐值（密钥）

**经验教训**：
- 京东商智的风控签名不是随机的，有固定算法
- 遇到风控拦截时，要从前端JS源码中找签名逻辑
- 关键JS文件：`commons-a5562705.js` 和 `vendors-6003f572.js`

---

### 坑2：openpyxl read_only模式读取Excel列数错误

**时间**：2026-08-04

**问题描述**：
用 `openpyxl(load_workbook(read_only=True))` 读取API返回的Excel文件，只识别到1列1行，但实际有46行×68列。

**解决方案**：
去掉 `read_only=True` 参数，用普通模式打开即可正确读取。

**经验教训**：
- openpyxl的 `read_only` 模式对某些京东导出的Excel文件兼容性不好
- 读取Excel数据用普通模式 `load_workbook(path)` 更可靠

---

### 坑3：导出的07-30数据与网页后台对不上（日期参数不同步）⚠️重大

**时间**：2026-08-05

**问题描述**：
商品流量来源 3渠道导出 2026-07-30 数据，与网页后台「流量来源页」导出对不上：行数不同、SKU列表不同、数值不同；且 07-29 与 07-30 两次导出数据完全相同。

**排查过程**：
1. 先怀疑 uuid前缀/Cookie/签名 → 用真实抓包对比后发现请求体完全一致（除风控参数）
2. 最终定位：程序发出的请求体是 `date=2026-07-30&startDate=2026-07-29&endDate=2026-07-29`
   —— `--date` 只覆盖了 date，startDate/endDate 仍回落 config.xlsx 里的旧日期（07-29）
3. 接口按 **startDate~endDate 区间**取数 → 返回 07-29 数据

**根本原因**：
`_get_date_params()` 中 `start_date = self.config.get("startDate", date)`：当命令行传新日期时，config 里旧的 startDate/endDate 被沿用，三日期不同步。

**解决方案**：
start/end 未显式传入时默认=date（三值一致），修复后 3渠道与网页导出行数/SKU/数值完全一致。

**经验教训**：
- date/startDate/endDate **三值必须一致**，接口按 start~end 区间取数
- `--date` 覆盖时必须同步三个日期，不能只改 date
- 数据"对不上"先抓真实请求对比 body，再查代码

---

### 坑4：pandas读取Excel自动把数字样式转int64，长SKU精度丢失

**时间**：2026-08-05

**问题描述**：
Mock回归验证时，16位SKU未按规则保留文本。根因：`pd.read_excel()` 默认把"数字样式"列自动推断为 int64，`safe_convert_numeric()` 的">15位保留文本"保护根本来不及执行。

**解决方案**：
`pd.read_excel(..., dtype=str, na_filter=False)` 先全部按文本读入，再统一走安全转换规则。

**经验教训**：
- 读取Excel做后处理前，必须 `dtype=str` 防pandas类型推断
- 数值安全转换的前提是"先拿到字符串"

---

### 坑5：京麦订单导出持续触发 `code=601"操作频繁，请稍后重试"`（⏸️ 项目暂停原因）

**时间**：2026-08-06

**问题描述**：
京麦订单明细【加密】导出项目，反复抓包（Chrome、Edge 各试多次）都收到 `code=601, "操作频繁，请稍后重试"`，页面显示"操作频繁，请稍后重试"。

**错误响应**：
```json
{"msg":"操作频繁，请稍后重试","code":601,"dsm-trace-id":"5522753.41661.17860037344037914"}
```

**错误判断**：
- ❌ 不是脚本 BUG（脚本抓包本身没报错，请求也正常发出）
- ❌ 不是 Cookie 失效（请求头里 Cookie 是新的）
- ❌ 不是 User-mnp 签名错误（属于京麦商智，京麦接口用 h5st 签名）
- ✅ 是**账号/IP 触发京东风控临时限流**

**支撑证据**：
1. 多次测试（Chrome 和 Edge）都收到同样 601
2. 切换浏览器后仍然 601（说明账号/IP 维度限流，不是浏览器维度）
3. 错误信息是"操作频繁"而非"签名错误"或"权限不足"
4. 京东内部限流通常有 30-120 分钟冷却

**解决方案**：
⏸️ **暂停项目，主动停止抓包**，避免加重风控标记。

**项目恢复前置条件**（必须全部满足）：
1. 收到明确的【项目恢复指令】
2. 账号冷却 30-120 分钟 **或** 更换干净公网 IP（手机热点）
3. 重新运行 `jd_cdp_capture.py` 抓到 `code=200` 成功响应
4. 拿到 5 个完整接口的成功报文后再开发 `jd_order_export.py`

**风控硬性约束**（恢复时必须遵守）：
1. 触发 601 后 30-120 分钟冷却；冷却期间任何导出请求都会重置冷却
2. 禁止多端并发操作同一账号的导出模块（脚本浏览器、网页京麦、京麦 APP）
3. 严禁使用代理/VPN/IP 池访问京麦商家后台（升级账号风险）
4. 冷却无效时换手机热点换公网 IP 再测试
5. h5st 签名必须由真实浏览器内部 JS 生成，禁止硬编码复制抓包得到的 h5st
6. 脚本不允许写自动循环重试 601 接口的逻辑
7. 抓包操作行为约束：登录成功后首页静置 1-2 分钟，再进入导出页面；操作动作放缓

**关联文件**：
- 抓包脚本：[jd_cdp_capture.py](jd_cdp_capture.py)
- 601 限流样本：`cdp_network_log.json`（17.5 MB，1641 条请求，仅 2 条 exportCenterService）
- 完整上下文：`main_old_backup.py` 第 723 行后追加【业务上下文备份 2026-08-06】区块
- 详细业务沉淀：`.trae/skills/jd-api-analyze/SKILL.md` 京麦模块项目1

**经验教训**：
- **抓包阶段触发风控 ≠ 脚本写错了**：要会区分"请求无应答/连接失败"和"请求返回业务错误码"
- **不要盲目重试**：反复重试会加重风控标记，导致更长时间封禁
- **保留限流样本**：601 错误响应也保留，对比后续是否接口变更
- **不同业务域风控规则不同**：商智（User-mnp MD5 签名）vs 京麦（h5st 一次性签名）vs 通用限流（601）
- **浏览器切换要清理用户配置目录**：Chrome 与 Edge 不可共用 `cdp_user_data/`，避免 Cookie/登录态污染

---

### 坑6：UUID 前缀"看起来固定但实际是随机的"（店铺来源-三级渠道项目，2026-08-07）

**时间**：2026-08-07

**问题描述**：
店铺来源-三级渠道离线流量报表项目，用户抓包两次：
- 抓包 1：`uuid=f1d5ae161b41f4153fc0-19fd685e6a1`
- 抓包 2（13 秒后）：`uuid=a31e066d8e94f4f39a3a-19fda02c2d4`

前缀完全不同 → 误以为"按业务域名有固定 prefix"，尝试复用项目1-3的"固定 prefix + 随机数"方案。

**根因**：
京东前端 SDK 在每次浏览器会话运行时动态生成 UUID（`window.$frontend_monitor.getUUID(url)`），**与商品搜索/购物车的"固定 prefix + 随机后缀"完全不同**。

**支撑证据**：
1. 两次抓包仅间隔 13 秒，前缀已经完全不同
2. 同会话内 uuid 后缀每次调用也变（说明每次会话内也是重新生成）
3. 与项目1-3（downSkuTable.ajax）的抓包模式不同——那里多次抓包 prefix 是固定的 `ca412182e5668a106054`

**解决方案**：
新业务 `OfflineChannelAPI` 自实现 `_gen_uuid_random()`，完全随机生成 16hex + 10hex，**不依赖基类 UUID_PREFIX**（与用户 2026-08-06 强调的"禁止硬编码 uuid"约束一致）。

```python
def _gen_uuid_random(self):
    import secrets
    prefix = secrets.token_hex(8)        # 8字节 = 16hex
    suffix = secrets.token_hex(5)        # 5字节 = 10hex
    return f"{prefix}-{suffix}"
```

**经验教训**：
- **UUID 策略必须分业务看**：项目1-3是"固定 prefix + 随机"，本项目是"完全随机"
- **不能盲目复用前一个项目的 UUID 生成逻辑**：即使是同一个 SDK，不同页面行为可能不同
- **多抓几次包对比 UUID**：如果前缀在两次抓包里都变了 → 完全随机；如果两次都固定 → 固定 prefix
- **遵守用户约束**：用户 2026-08-06 明确说"uuid 每次动态生成，禁止硬编码"——本项目必须完全随机化，不能有任何"固定前缀兜底"

---

### 坑7：商智搜索/推荐/购物车接口服务端不支持多日区间导出（2026-08-10）⚠️重大

**时间**：2026-08-10

**问题描述**：
用户导出区间 2026-08-03~2026-08-09 的数据，但报表里的日期列只填了 `2026/8/9`，8月3日~8月8日对应日期的数据都不见。怀疑"是不是被统一归类到了8月9日"。

**排查过程**：
1. 先核验：`downSkuTable.ajax` 表单里**确实有** `startDate` / `endDate` 字段（且 2026-08-05 修复过三值同步）
2. 但用户抓包 + 真实接口验证后发现：**服务端只返回单日数据**——传多日区间，实际只取一天（区间相关参数形同虚设）
3. 二次实证（`--range last_3d` 真实跑通）：3天区间拆成逐天后共 117 行×69 列，日期分布 45/38/34 对应 08-07/08/08/08-09，**每天数据各自完整**，验证逐日拆分方案正确

**根本原因**：
京东商智搜索/推荐/购物车接口（downSkuTable.ajax）**服务端不支持多日区间导出**——表单有 startDate/endDate 字段，但后端实际只按单日取数。区间查询若直接传区间，会丢数据且日期归属混乱。

**解决方案**（P0 用户决策 2026-08-10）：
1. 外层 CLI 参数不动，内部做拆解：`split_date_range()` 把区间拆成逐天日期列表（模块级纯函数，含格式校验 / start≤end / **最大31天上限**，超限抛 ValueError 且不发请求）
2. `_download_sku_by_days()` 逐天 for 循环：每天 `date=startDate=endDate=当天` 调接口 → 单天数据插入当天日期列 → `pd.concat` 合并 → 一次性输出合并 xlsx
3. 文件名标注区间（如 `搜索流量_2026-08-03_2026-08-09.xlsx`），输出子目录用最后一天 end_date；中间单天文件不落盘
4. 单日期不进入循环，保持原有执行路径不变
5. CLI 警告输出：接口不支持区间，自动拆分逐日循环 + 预计耗时提示
6. 配置注释（P1-1）+ mock 单测（P1-3，test_flow_sku_range_unittest.py 13/13 通过）

**附加踩坑（build_business_output_path 双日期文件名校验误区）**：
- 区间合并文件名 `搜索流量_2026-08-03_2026-08-09.xlsx` 含**两个日期**（start_end），
  而复用公共函数 `build_business_output_path()` 的"去掉 `_{date}.xlsx` 后缀提取业务模块名"规则时，
  只会去掉末尾 `_2026-08-09.xlsx`，把 start 日期 `2026-08-03` 误并入业务模块名，
  产生错误目录 `output/搜索流量_2026-08-03/2026-08-09/`。
- 修复：区间场景**不能复用该公共函数**，直接构造目录 `output/{业务模块}/{end_date}/{filename}`。

**经验教训**：
- **接口文档/表单字段 ≠ 服务端能力**：表单有 startDate/endDate 不代表支持区间，必须真实抓包实证
- **区间查询先验证数据完整性**：逐日拆分后按天核对行数/日期分布，确认无丢数据
- **文件名约定不能默认"只有一个分隔符"**：含双日期的文件名会让"去掉后缀提取模块名"的逻辑误判，特殊文件名要单独处理，不能盲目复用公共函数

---

### 坑8：商智关键词分析项目 关键实证要点归档（2026-08-10 上线）

**时间**：2026-08-10

**背景**：项目13「商智关键词分析」由另一会话开发并推送（远程提交 `cd47f7b`：同步 xlsx 流 + UUID 完全随机 + day/month 双粒度），本次 pull 同步本地后归档。以下要点全部来自抓包实证（非 bug 修复，属易错点预防）。

**关键实证/易错点**：
1. **date 字段格式随粒度变化**：
   - day 粒度：`date=YYYY-MM-DD`（带分隔符，如 2026-07-30）
   - month 粒度：`date=YYYYMM`（**紧凑无分隔符**，如 202607）
   - 不能统一用一种格式，否则服务端可能按错误粒度取数
2. **dateType/interval 字段顺序随粒度变化**（Python dict 保留插入顺序，必须精确匹配抓包）：
   - month 抓包：`dateType=month, interval=MONTH`（**dateType 在前**）
   - day 抓包：`interval=DAY, dateType=day`（**interval 在前**）
   - 代码 `_get_date_params` 两个粒度各起一段 dict 保证顺序
3. **该接口服务端支持区间聚合**：与项目1 搜索/推荐/购物车**不同**（那些接口传区间只返单日数据，需要逐日拆分）；关键词分析传区间由**服务端聚合**，**不需要**逐日拆分循环——同一 downTable 家族接口能力不同，不能想当然复用拆分逻辑
4. **原生 Excel 无时间列**：需 pandas 读取后手动插入时间区间列（day → 插入「日期」列 YYYY/M/D；month → 插入「日期范围」列 YYYY/M/D ~ YYYY/M/D）
5. **UUID 完全随机**：与项目4 同源（secrets.token_hex(8) + "-" + token_hex(5)），不依赖基类 UUID_PREFIX
6. **同步 xlsx 流**：无 taskId、无轮询，一步拿到字节流（区别于京准通异步三步流程）

**经验教训**：
- **同域同家族接口能力可能不同**：区间支持、UUID 策略、同步/异步都要按接口实际抓包实证，禁止套用其他项目的假设
- **请求参数字段顺序也可能被服务端校验**：dict 插入顺序需精确匹配抓包，改参数时注意顺序
- **粒度参数与日期格式必须配套验证**：day/month 的 date 格式和字段顺序绑定，改粒度时同步检查

---

## 分歧记录

（后续对话中如有分歧，在此追加记录）

---

## 更新记录

| 日期 | 记录内容 |
|------|----------|
| 2026-08-04 | 创建文档，记录风控签名和Excel读取两个坑 |
| 2026-08-05 | 追加坑3（日期参数不同步→数据对不上）、坑4（pandas长数字精度丢失） |
| 2026-08-06 | 追加坑5（京麦601风控限流，导致订单明细【加密】导出项目暂停归档） |
| 2026-08-07 | 追加坑6（UUID完全随机 vs 项目1-3的固定prefix+随机策略，店铺来源-三级渠道项目必须独立实现完全随机UUID） |
| 2026-08-10 | 追加坑7（商智搜索/推荐/购物车接口服务端不支持多日区间导出→逐日拆分循环解决；含 build_business_output_path 双日期文件名误判的附加踩坑） |
| 2026-08-10 | 追加坑8（商智关键词分析项目13 上线归档：date 格式/dateType-interval 字段顺序随粒度变化、服务端支持区间聚合无需逐日拆分、原生无时间列需插入等实证要点） |
| 2026-08-11 | 追加坑9（京麦订单明细项目14 上线归档：①5步异步链路区别于商智同步/京准通3步 ②msoffcrypto双层加密 ③smsSendTip脱敏 ④每次任务密码不同 ⑤IMAP中文主题编码 ⑥xlrd不支持password参数 ⑦任务Id必须按时间匹配 ⑧IMAP兜底正则 ⑨不允许复用旧任务密码 ⑩不同店铺短信接收号动态变化） |
